{{#if next}}
    <button class="pagination" data-url="{{page_url next}}">
        <span class="load-more u-underline">Load More</span>
        <span class="page-number">Page {{page}} of {{pages}}</span>
    </button>
{{/if}}